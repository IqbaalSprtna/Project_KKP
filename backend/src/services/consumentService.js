const prisma = require("../utils/prisma");

const findAll = async (params) => {};

const findById = async (params) => {};

const createConsument = async (params) => {};

const updateConsument = async (params) => {};

const deleteConsument = async (params) => {};

module.exports = {
  findAll,
  findById,
  createConsument,
  updateConsument,
  deleteConsument,
};
